// Your code goes here

document.addEventListener("DOMContentLoaded", function () {
    const paragraph = document.querySelector("p"); // Selects the <p> tag
    if (paragraph) {
        paragraph.textContent = "This is really cool!"; // Updates the text content
    }
});